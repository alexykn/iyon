# S5 application external-consumer record

**1. Scope statement.** Convert `alexykn/iyon` from the S2–S4 local compatibility checkout into an external consumer of the standalone TUI repository. Pin the exact Rust and TypeScript TUI revision, remove local generic TUI source/tests/benches/ABI tooling, split the remaining native addon into the application-only `iyon-core-native`, and preserve application behavior and the direct-FFI TUI transport.

**2. Commits/repository SHAs.** TUI cleanup/public package commit: `e322f10dff490c1423d988982c0782c22774f85d`. Application implementation commit: `947ce35b01117efa93e60ac93a1f621db8f3baf2`; clean-checkout TypeScript path fix: `03b4d99aec23bf34427fa7ea34eb43390a8e7e0e`. Rust and TypeScript both pin the TUI revision `e322f10dff490c1423d988982c0782c22774f85d`.

**3. Review findings and corrections.** (a) The application still had the S2 local `crates/iyon-tui` checkout, TUI subtree under `packages/iyon-runtime`, TUI benchmarks/ABI tests, and generated TUI ABI tooling. All were deleted; the authoritative copies remain in `alexykn/iyon-tui`. (b) The mixed `crates/iyon-native` addon contained generic View ABI/History/TextStream/ViewSlot/ScrollPane exports beside kernel/session/tool/credential exports. It is now `crates/iyon-core-native`; all TUI modules, generated ABI files, header, TUI tests, and TUI artifact paths are gone. (c) `packages/iyon-runtime/src/native.ts` was rewritten as the application-only `NativeCoreAddon` contract and loads `iyon-core-native.node`; no shared `NativeAddon` name remains. (d) Runtime/plugin/application imports use public `@iyon/tui`; `iyon:tui` is only an application-owned alias that re-exports that package. Deep imports into TUI source/ABI/native internals were removed, including tool-renderer tests that previously inspected `nodeForBridge`; they now assert public View results. (e) The external package owns its native staging command; the application invokes the package-owned `iyon-tui-native-stage` bin before tests/builds and stages its own core addon separately. (f) API-surface configuration now scans only application crates; TUI Rust/API/ABI snapshots and local TUI evidence were removed from the application checkout. No CI file was modified.

**4. Implementation summary.** `Cargo.toml` removes the local TUI workspace member and pins `iyon-tui` to the exact Git revision; `Cargo.lock` records the same source. Root/Bun package manifests and consuming package manifests pin `@iyon/tui` to `github:alexykn/iyon-tui#e322f10dff490c1423d988982c0782c22774f85d`. `packages/iyon-runtime` no longer exports `@iyon/runtime/tui`; its virtual module maps `iyon:tui` to `@iyon/tui`. `packages/iyon-runtime/native/iyon-core-native.node` is the only application native artifact. `tools/ownership/check.ts` now enforces absence of local TUI compatibility paths, exact Rust/TypeScript pins, core-native purity, and public TUI imports.

**5. Provenance.** The original TUI repository history remains canonical and unrewritten. The application repository retains S2 extraction provenance (`SOURCE-SHA-MAP.jsonl`, `EXTRACTION-PROVENANCE.json`, and `S2-APPLICATION-EXTRACTION-RECORD.md`); the normative handoff remains only in `alexykn/iyon-tui`. The application keeps its product-owned legacy `crates/iyon/src/tui/**` surface while it exists, but it no longer vendors the generic `iyon-tui` crate.

**6. Gate evidence with actual counts/numbers.** `bun install --ignore-scripts` resolves the exact Git package; package-owned external TUI staging PASS for darwin-arm64; core-native staging/load PASS for darwin-arm64; `bun run typecheck` PASS; `bun run check:ownership` passes 5/5 application gates; `cargo run -q -p api-surface -- check --config tools/api-surface/surface.toml` reports reachable 799, mapped 799, missing 0, stale 0; `cargo test --workspace` passes 209 Rust tests across 25 test targets; application native/runtime probes pass 23/23 focused tests; plugin/tool/public TUI tests pass 53/53 focused tests; the full application Bun suite passes 280/281 tests with one unchanged S0-known failure, `Recovery Round 3 production path > production_successful_ls_is_green_finished`, caused by the existing `row must fit in u16` baseline path. A fresh clone at `03b4d99` with no local TUI checkout passes install, both native staging commands, ownership, typecheck, standalone build, and 7/7 external-consumer tests (14 expect calls). No CI result is claimed.

**7. Status.** **S5 COMPLETE.** The application builds and tests against exact external TUI package and Rust revisions, the core and TUI native addons are independent, no local generic TUI compatibility path remains, and no shared giant native contract exists. S6 is next: qualify the safe generated N-API lowering in the standalone TUI repository.
